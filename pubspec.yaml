name: flutter_pim
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.2.1
  geocoding: ^3.0.0
  geolocator: ^13.0.2
  http: ^1.3.0
  mysql1: ^0.20.0
  permission_handler: ^11.4.0
    
    
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
    uses-material-design: true

    assets:
     - android/imagens/logo.png
     - android/imagens/fundo.png
     - .env

  